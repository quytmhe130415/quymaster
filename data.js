var Food = require('./Food');
var data = [];
// var banhmi = new Food('meat', 2000);
// console.log(banhmi);
for (let index = 0; index < 1000; index++) {
    let hambergur = new Food('hambergur' + index, 1000 + index);
    data.push(hambergur);
    this.image
}
module.exports = data;